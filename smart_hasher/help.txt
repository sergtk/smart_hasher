[TODO] -help
	this information

[TODO] -calcWorkflow
	skipIfExists (default)
		If hash already calculate then file is skipped. This is very useful for large files after interruption. The hash calculation can just be continued
	updateIfExists
		If hash exists, then it is simply updated

[TODO] -hashFileNameOutputPostfix <value>
	Specify hash file postfix. This is to specify for different contextes, e.g. if file name ends with ".md5", then it ends with "md5.<value>"

[TODO] -hashOutputTarget
		fileAndConsole (default)
		file
		console
